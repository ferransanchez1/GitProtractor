{
  "name": "ProtractorTutorial",
  "dependencies": {
    "protractor": "^5.3.0",
    "jasmine-data-provider": "^2.2.0",
    "protractor-jasmine2-html-reporter": "0.0.7"   
  },
  "scripts": {
    "webdriver-update": "./node_modules/.bin/webdriver-manager update",
    "webdriver-start": "./node_modules/.bin/webdriver-manager start",
    "protractor": "protractor configuration.js",
    "SmokeSuite": "protractor configuration.js --suite Smoke",
    "RegressionSuite": "protractor configuration.js --suite Regression",
    "SanitySuite": "protractor configuration.js --suite Sanity",
    "start": "npm run webdriver-manager update &&npm run webdriver-manager start"        
  }
}
